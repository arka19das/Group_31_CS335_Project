struct pt{
    int a;
    int b;
    int arr[10];
    
};
int func(int n)
{
    int r;
    return r;
}
int main()
{
    int sz = 3;
    int arr[3];
    // print_int(sz);
    for (int i = 0; i < sz; ++i) {
        read_int(i);
    }
    for (int i = 0; i < sz; ++i) {
        print_int(i);
    }
    // func(a);
    return 0;
}
